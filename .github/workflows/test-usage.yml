name: docker-compose usage workflow
on: push
jobs:
  test-usage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build the stack
        run: docker-compose -f docker-compose.yml up -d
      - name: Test
        run: docker exec pytraction_pytraction_1 python scripts/usage.py